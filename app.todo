1. Add progress bar for pages
    - Asked in Vue forums how to use the global $Progress from setup.
    - Everything else is installed and set up.
    - Just need to add to Router navigation guard to trigger $Progress.

2. 